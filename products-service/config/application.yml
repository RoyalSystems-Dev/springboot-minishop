server:
  port: 8082

spring:
  application:
    name: products-service

  cloud:
    stream:
      default-binder: nats
      binders:
        nats:
          type: nats
          environment:
            spring:
              cloud:
                stream:
                  nats:
                    servers: nats://nats:4222
